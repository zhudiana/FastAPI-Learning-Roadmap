## 🖥 Working with the Terminal / Command Line

When working with Python, you’ll often use the terminal (also called command line, shell, or CLI) to run scripts, check versions, and manage files.

### 1. Basic Terminal Navigation

- `pwd` → Show the current directory (Mac/Linux)
- `cd folder_name` → Change directory
- `cd ..` → Move up one folder
- `ls` (Mac/Linux) or `dir` (Windows) → List files and folders in the current directory

### 2. Running Python

- `python --version` → Check the installed Python version
- `python file.py` → Run a Python file
- `python -i file.py` → Run a Python file and stay in interactive mode
- `python` → Start the Python REPL (interactive shell)
- `exit()` or Ctrl+D → Exit the Python REPL

### 3. Installing and Managing Packages

- `pip install package_name` → Install a package
- `pip uninstall package_name` → Remove a package
- `pip list` → Show installed packages
- `pip freeze > requirements.txt` → Save a list of installed packages to a file
- `pip install -r requirements.txt` → Install all packages listed in a file

### 4. Helpful Shortcuts

- Ctrl+C → Stop a running process
- Arrow keys ↑ ↓ → Browse through command history
- Tab → Auto-complete file and folder names

---

📌 Why this matters:  
You’ll use the terminal to run Python scripts, manage dependencies, and navigate your project files efficiently.
